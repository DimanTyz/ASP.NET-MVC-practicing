
@{
    ViewBag.Title = "DB";
    var grid = new WebGrid(Model);
}

<h2>DB</h2>

@grid.GetHtml(
    columns: grid.Columns(grid.Column(columnName: "Login", header: "Имя"),
   grid.Column(columnName: "Password", header: "Пароль"),
    grid.Column(columnName: "Age", header: "Возраст")));
    


